-- Show the first 10 elements of the agency table
SELECT * FROM agency LIMIT 10;

-- Show the first 10 elements of the loan_type table
SELECT * FROM loan_type LIMIT 10;

-- Show the first 10 elements of the property_type table
SELECT * FROM property_type LIMIT 10;

-- Show the first 10 elements of the loan_purpose table
SELECT * FROM loan_purpose LIMIT 10;

-- Show the first 10 elements of the owner_occupancy table
SELECT * FROM owner_occupancy LIMIT 10;

-- Show the first 10 elements of the preapproval table
SELECT * FROM preapproval LIMIT 10;

-- Show the first 10 elements of the action_taken table
SELECT * FROM action_taken LIMIT 10;

-- Show the first 10 elements of the msamd table
SELECT * FROM msamd LIMIT 10;

-- Show the first 10 elements of the state table
SELECT * FROM state LIMIT 10;

-- Show the first 10 elements of the county table
SELECT * FROM county LIMIT 10;

-- Show the first 10 elements of the applicant_ethnicity table
SELECT * FROM applicant_ethnicity LIMIT 10;

-- Show the first 10 elements of the co_applicant_ethnicity table
SELECT * FROM co_applicant_ethnicity LIMIT 10;

-- Show the first 10 elements of the applicant_race table
SELECT * FROM applicant_race LIMIT 10;

-- Show the first 10 elements of the co_applicant_race table
SELECT * FROM co_applicant_race LIMIT 10;

-- Show the first 10 elements of the applicant_sex table
SELECT * FROM applicant_sex LIMIT 10;

-- Show the first 10 elements of the co_applicant_sex table
SELECT * FROM co_applicant_sex LIMIT 10;

-- Show the first 10 elements of the purchaser_type table
SELECT * FROM purchaser_type LIMIT 10;

-- Show the first 10 elements of the denial_reason table
SELECT * FROM denial_reason LIMIT 10;

-- Show the first 10 elements of the hoepa_status table
SELECT * FROM hoepa_status LIMIT 10;

-- Show the first 10 elements of the lien_status table
SELECT * FROM lien_status LIMIT 10;

-- Show the first 10 elements of the edit_status table
SELECT * FROM edit_status LIMIT 10;

-- Show the first 10 elements of the location table
SELECT * FROM location LIMIT 10;

-- Show the first 10 elements of the final_table
-- SELECT * FROM final_table LIMIT 10;